[project]
name = "custom-shell"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psutil>=7.1.0",
    "py-cpuinfo>=9.0.0",
    "rich>=14.1.0",
    "tester",
    "wmi>=1.5.1",
]

[tool.uv.sources]
tester = { path = "../Testing-Suite/package/tester" }

[tool.uv.workspace]
members = [
    "SSHTST",
]
